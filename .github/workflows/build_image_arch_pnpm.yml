name: Build Arch image pnpm* tag creation

concurrency:
  group: arch-pnpm-image-build
  cancel-in-progress: false

on:
  push:
    tags:
      - 'pnpm*'
  workflow_dispatch:

env:
  DOCKER_USERNAME: namesmt

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: amd64,arm64

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PAT }}

      - name: Container Details
        id: container_detail
        run: |
          if [ "$GITHUB_EVENT_NAME" == "workflow_dispatch" ]; then
            TAG=$(git tag --sort=-v:refname | head -1)
          else
            TAG=$(echo ${{ github.ref }} | cut -d/ -f3)
          fi

          # Use the repository name as the image name for simplicity
          IMAGE_NAME="$(echo ${GITHUB_REPOSITORY} | cut -d: -f1 | tr '[:upper:]' '[:lower:]')"

          # Set output parameters.
          echo "imageName=${IMAGE_NAME}" >> $GITHUB_OUTPUT
          echo "suffix=${TAG}" >> $GITHUB_OUTPUT
          echo "pnpmVersion=${TAG:4}" >> $GITHUB_OUTPUT

      - name: Build arch-node
        uses: docker/build-push-action@v6
        with:
          file: ./arch/arch-node.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          # Also push to old docker hub repo: `namesmt/images-arch`
          tags: |
            namesmt/images-arch:node
            ${{ steps.container_detail.outputs.imageName }}:arch-node
            ${{ steps.container_detail.outputs.imageName }}:arch-node_${{ steps.container_detail.outputs.suffix }}
          build-args: |
            PNPM_VERSION=${{ steps.container_detail.outputs.pnpmVersion }}

      - name: Build arch-node-dev
        uses: docker/build-push-action@v6
        with:
          file: ./arch/arch-node-dev.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            namesmt/images-arch:node-dev
            ${{ steps.container_detail.outputs.imageName }}:arch-node-dev
            ${{ steps.container_detail.outputs.imageName }}:arch-node-dev_${{ steps.container_detail.outputs.suffix }}
          build-args: |
            PNPM_VERSION=${{ steps.container_detail.outputs.pnpmVersion }}

      - name: Build arch-node-aws
        uses: docker/build-push-action@v6
        with:
          file: ./arch/arch-node-aws.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            namesmt/images-arch:node-aws
            ${{ steps.container_detail.outputs.imageName }}:arch-node-aws
            ${{ steps.container_detail.outputs.imageName }}:arch-node-aws_${{ steps.container_detail.outputs.suffix }}
          build-args: |
            PNPM_VERSION=${{ steps.container_detail.outputs.pnpmVersion }}

      - name: Build arch-node-aws-dev
        uses: docker/build-push-action@v6
        with:
          file: ./arch/arch-node-aws-dev.Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            namesmt/images-arch:node-aws-dev
            ${{ steps.container_detail.outputs.imageName }}:arch-node-aws-dev
            ${{ steps.container_detail.outputs.imageName }}:arch-node-aws-dev_${{ steps.container_detail.outputs.suffix }}
          build-args: |
            PNPM_VERSION=${{ steps.container_detail.outputs.pnpmVersion }}
